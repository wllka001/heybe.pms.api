export enum Currency {
  USD = 'USD',
}

export enum PaymentMethod {
  EVC = 'evc',
  MERCHANT = 'merchant',
  BANK = 'bank',
}

export enum PaymentStatus {
  RECORDED = 'recorded',
  VERIFIED = 'verified',
  RECONCILED = 'reconciled',
  REJECTED = 'rejected',
  REVERSED = 'reversed',
}
